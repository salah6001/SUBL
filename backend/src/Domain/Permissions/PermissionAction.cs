namespace Domain.Permissions;

/// <summary>
/// Defines the possible actions for permissions.
/// </summary>
public enum PermissionAction
{
    Create = 1,
    Read = 2,
    Update = 3,
    Delete = 4
}
